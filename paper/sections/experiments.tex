% === VI. EXPERIMENTS AND RESULTS =============================================
\section{Experiments and Results}
\label{sec:results}

\subsection{Setup}

All experiments run in simulation on the architecture of
Section~\ref{sec:platform}: the Formula AI vehicle in CARLA~\cite{carla2017},
driven through \bridge{} on a ROS~2 Humble workspace, with the CARLA server in
synchronous mode at a \SI{0.033}{\second} fixed physics step. Identification
data is collected over \SI{30}{\second} of pure-pursuit driving at
\SI{50}{\hertz}, matching the baseline's data budget~\cite{ontrack2024}. Ground
truth for forces, slip angles and normal loads is the simulator's own per-wheel
telemetry.

\subsubsection{Reference trajectory}
\label{subsubsec:raceline}
Every run follows the same optimised raceline (Fig.~\ref{fig:raceline}),
produced with the TUM \texttt{global\_race\-trajectory\_optimization}
package~\cite{tumracetraj} under its minimum-curvature
formulation~\cite{heilmeier2020} on a recorded centreline of the CARLA
\texttt{silverstone} circuit: a closed \SI{5830}{\meter} lap of 2916 waypoints
at \SI{2.0}{\meter} spacing, reference speed
\SIrange{14.90}{46.02}{\meter\per\second}, minimum corner radius
\SI{18.7}{\meter} ($|\kappa|_{\max}=\SI{0.0536}{\per\meter}$).

Two of the optimiser's inputs matter for Section~\ref{subsec:negative}. Its
vehicle configuration assumes a mass of \SI{204}{\kilo\gram} against the
\SI{269.6}{\kilo\gram} the physics engine simulates, and its \texttt{ggv}
envelope declares a \emph{constant} lateral capability of
\SI{12.0}{\meter\per\second\squared} at every speed. The resulting profile
saturates that envelope exactly---peak demand $\max(v_{x}^{2}|\kappa|)$ over
the lap is \SI{12.00}{\meter\per\second\squared}, i.e.\ \SI{1.22}{\g}---so the
trajectory is feasible \emph{by construction} for the vehicle the optimiser was
told about, and only for that vehicle. Against the plant's measured
steady-state ceiling of $\approx\SI{9.8}{\meter\per\second\squared}$,
\SI{11.9}{\percent} of the lap is infeasible (Fig.~\ref{fig:raceline}c).

\begin{table}[t]
\caption{Reference tire model of the plant, obtained by fitting the Magic
Formula directly to the simulator's per-wheel telemetry over a dedicated
limit-excitation run reaching $\alpha=\SI{0.37}{\radian}$, axle $\mu=1.05$ and
\SI{1.50}{\g}. Differential evolution, $E$ constrained to $\ge-2$.}
\label{tab:ref}
\centering
\small
\begin{tabular}{@{}lccccc@{}}
\toprule
Axle & $B$ & $C$ & $D$ & $E$ & RMSE\\
\midrule
Front & 7.076 & 1.346 & 1.009 & $-2.000$ & \SI{135}{\newton}\\
Rear  & 7.873 & 1.383 & 1.002 & $-1.024$ & \SI{85}{\newton}\\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:ref} is the reference against which the on-track identifier is
scored. Note $D\approx1.0$, not the \num{1.5} of the wheel material
coefficient---the distinction of Section~\ref{subsec:platform}.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{raceline.pdf}
\caption{The optimised raceline used for every experiment, generated with the
TUM \texttt{global\_racetrajectory\_optimization} package~\cite{tumracetraj}
under its minimum-curvature formulation~\cite{heilmeier2020} from a recorded
centreline of the CARLA \texttt{silverstone} circuit. (a) Track layout,
coloured by reference speed; the marked corner ($R=\SI{18.7}{\meter}$) sets the
feasibility limit. (b) Reference speed profile,
\SIrange{14.90}{46.02}{\meter\per\second}. (c) Lateral demand
$v_{x}^{2}|\kappa|$ along the lap. The profile saturates the constant
\SI{12.0}{\meter\per\second\squared} envelope given to the optimiser (dashed)
but not the grip this vehicle has (dotted, $\approx\SI{1.0}{\g}$, measured):
the shaded segments, \SI{11.9}{\percent} of the lap, demand more than the plant
can deliver. This is the trajectory the corrected identifier's admissibility
gate rejects.}
\label{fig:raceline}
\end{figure*}

\subsection{The failure mode, reproduced}
\label{subsec:failure}

Running the baseline pipeline unmodified on this platform yields
\begin{align*}
\mathrm{C\_Pf} &= [5.2874,\; 1.5536,\; \mathbf{0.4},\; \mathbf{1.0}],\\
\mathrm{C\_Pr} &= [19.3236,\; \mathbf{1.2},\; 0.4046,\; -0.1292],
\end{align*}
with the bold entries exactly on their box constraints and $B_{r}$ just under
its upper bound: five of eight coefficients on or against a rail. The implied
physics is qualitatively wrong, not merely inaccurate---a grip ceiling of
\SI{3.95}{\meter\per\second\squared} (\SI{0.40}{\g}) and a front axle stiffness
of \SI{4012}{\newton\per\radian}, so the model believes it needs
$\approx\SI{0.30}{\radian}$ of front slip to generate \SI{1}{\g}, beyond the
vehicle's \SI{0.2793}{\radian} steering limit. The model believes the car
cannot corner. Table~\ref{tab:closedloop} shows why this stays invisible until
it is not: lateral demand scales as $v^{2}$.

\begin{table}[t]
\caption{Closed-loop consequence of the degenerate tire set, measured in a
harness driving the raceline for \SI{20}{\second} with the
pure-pursuit$\rightarrow$MPC switch at $t=\SI{5}{\second}$.}
\label{tab:closedloop}
\centering
\small
\begin{tabular}{@{}lcc@{}}
\toprule
Controller model & Max cross-track & Infeasible stages\\
\midrule
Plausible prior ($D=1.5$)        & \SI{0.03}{\meter}  & 0\\
Identified set @ \SI{11.1}{\meter\per\second} & \SI{0.16}{\meter} & 0\\
Identified set @ \SI{15}{\meter\per\second}   & \SI{0.43}{\meter} & 0\\
Identified set @ \SI{27}{\meter\per\second}   & \SI{41.89}{\meter} & 29\,461\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Isolating the cause}
\label{subsec:isolation}

Four hypotheses were tested against measurement rather than argued from code.
Table~\ref{tab:hypotheses} records the outcome, including the two that were
wrong; the eliminated hypotheses are as informative as the confirmed one.

\begin{table}[t]
\caption{Hypotheses tested against the simulator's own telemetry.}
\label{tab:hypotheses}
\centering
\scriptsize
\begin{tabular}{@{}p{3.05cm}p{4.5cm}@{}}
\toprule
Hypothesis & Verdict\\
\midrule
Friction warm-start measures utilised friction over a low-slip mask &
\emph{Structurally confirmed, not the cause.} Utilisation measures \num{0.044}
median, \num{0.46} peak at \SI{13}{\meter\per\second}. Corrected anyway
(Sec.~\ref{subsec:measurement}).\\
Static $D$ prior below its own lower bound &
\emph{Confirmed, secondary.} Corrected; multi-starts 1$\rightarrow$8.\\
Only $BCD$ identifiable at low slip &
\emph{Confirmed, and it is the mechanism}---but caused by the rollout speed,
not by the recorded lap (Sec.~\ref{subsec:excitation}).\\
\texttt{/odom} twist in world frame &
\emph{Refuted.} corr $=0.93$, RMS \SI{0.09}{\meter\per\second} against a
body-frame interpretation.\\
Steady-state force model~\eqref{eq:ssforces} inadequate &
\emph{Refuted as a cause.} corr $=0.993$, slope \num{0.94} against the
simulator's own axle force.\\
$\delta$ taken from command, not achieved &
\emph{Confirmed, significant.} Ratio \num{0.76}; worth \SI{20}{\percent} of
front stiffness.\\
\bottomrule
\end{tabular}
\end{table}

The decisive experiment is an offline round-trip with the residual network
zeroed, so that only the rollout is under test: a synthetic $D=1.5$ tire,
rolled out at \SI{4}{\meter\per\second} and re-fitted, returns
$D_{f}=0.753$, $D_{r}=0.737$ with three coefficients railed; the same tire
rolled out at \SIrange{8}{20}{\meter\per\second} returns
$D=\numrange{1.40}{1.61}$ with nothing railed. Since the tire is known exactly
and the residual model is absent, this isolates the rollout configuration as
the cause and confirms \eqref{eq:mureach}. Figure~\ref{fig:baseline} shows the
round trip as a sweep over rollout speed, next to the reachable-friction bound
that predicts it and the slip-angle coverage each configuration delivers to
the fit.

\subsection{Baseline versus this work}
\label{subsec:baselinecmp}

Table~\ref{tab:baseline} places the pipeline as published~\cite{ontrack2024}
beside the pipeline as it stands here, on the axes that changed. The residual
model, the data budget, the four-stage loop and the steady-state force
recovery are inherited unchanged; what differs is the configuration of the
virtual-data step, the regularisation topology, the measurement chain, and the
contract at the output.

\begin{table}[t]
\caption{The baseline pipeline~\cite{ontrack2024} against this work, on the
axes that differ. Rows marked \emph{inherited} are unchanged and are listed so
that the extent of the modification is unambiguous.}
\label{tab:baseline}
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{1.95cm}p{2.55cm}p{3.0cm}@{}}
\toprule
Axis & Baseline~\cite{ontrack2024} & This work\\
\midrule
Platform & 1:10, \SI{3.7}{\kilo\gram}, $L=\SI{0.32}{\meter}$ &
Full scale, \SI{269.6}{\kilo\gram}, $L=\SI{1.53}{\meter}$\\
Rollout speed & $\mathrm{clip}(\overline{v_{x}},2,4)\,\si{\meter\per\second}$ &
From the measured lap, floored at \SI{7}{\meter\per\second}\\
Reachable $\mu$ & $\approx2.0$ (adequate) & $0.43$ under the baseline clip;
$\ge1.2$ after correction\\
Sweep amplitude & Fixed \SI{0.4}{\radian} &
$\mathrm{clip}(1.5P_{99}(|\delta_{\mathrm{obs}}|),\delta_{\min},\delta_{\max})$\\
Regularisation & None; each fit is the next start point &
Tikhonov to a \emph{frozen} prior; start point still tracks\\
Steering signal & Command & Achieved road-wheel angle\\
Mass, geometry & Configuration file & Measured static wheel loads\\
Friction prior & --- & $P_{99}$ of measured $|a|/g$, as a floor\\
Solver & Trust-region, single start & Multi-start, plus simplex and global
options\\
Output contract & Per-coefficient box constraints & Gates on derived
quantities at the controller interface\\
\addlinespace
Residual model & MLP, 58 parameters & \emph{inherited} (with selectable
alternatives)\\
Data budget & \SI{30}{\second}, one controller-free lap & \emph{inherited}\\
Force recovery & Quasi-steady yaw balance & \emph{inherited}\\
Ground truth & Steady-state manoeuvre in a large space &
Simulator per-wheel force, slip and load\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Identification after correction}
\label{subsec:corrected}

With the six defects of Table~\ref{tab:defects} corrected, three consecutive
live identifications return no coefficient on a bound. Peak friction
coefficients land at $D_{f},D_{r}\in[1.05,1.18]$ against the reference
\num{1.009}/\num{1.002} of Table~\ref{tab:ref}; axle cornering stiffnesses land
at $C_{f}\approx\SI{21}{\kilo\newton\per\radian}$ and
$C_{r}\approx\SI{20}{\kilo\newton\per\radian}$ against the simulator's measured
\SIrange{17}{19}{\kilo\newton\per\radian}; the understeer gradient has the
correct sign ($l_{f}C_{f}<l_{r}C_{r}$) on every run, matching the measured
plant; and one-step prediction on held-out data gives $R^{2}=\num{0.97}$ for
$v_{y}$ and $R^{2}=\num{0.99}$ for $\omega$. The peak-friction drift across the
six co-identification iterations is removed: where the unbounded sweep walked
$D$ from \num{1.01} to \num{1.87}, the bounded sweep with a frozen prior
settles inside $[1.05,1.18]$.

\begin{table*}[t]
\caption{Defects identified, corrected and regression-tested in this work. The
effect column reports the measured change where one was quantified.}
\label{tab:defects}
\centering
\small
\begin{tabular}{@{}p{3.1cm}p{5.4cm}p{4.1cm}p{3.6cm}@{}}
\toprule
Defect & Root cause & Correction & Measured effect\\
\midrule
Degenerate fit, $D$ on its bound &
Pacejka fit runs on a synthetic rollout whose speed was clipped to
\SIrange{2}{4}{\meter\per\second}; $\mu_{\mathrm{reach}}=0.43$ &
Rollout speed from measured lap speed, floored at
\SI{7}{\meter\per\second}; warning when \eqref{eq:mureach} $<D$ &
$D$: 0.40 (railed) $\rightarrow$ 1.05--1.18; $C_{f}$: 4.0 $\rightarrow$
21 \si{\kilo\newton\per\radian}\\
\addlinespace
Identified set never reached the controller &
Trainer wrote coefficients to file but returned nothing; the node re-read a
static prior file and submitted \emph{that} &
Trainer returns the identified coefficients; node submits them &
Service received the prior verbatim while the fit had produced a different
set\\
\addlinespace
$D$ drifts upward across iterations &
Sweep extrapolated the residual network far past its training range, and each
answer became its own regularisation target &
Sweep bounded at $1.5\times P_{99}(|\delta|)$; regularisation target frozen at
the configured prior &
1.01 $\rightarrow$ 1.87 drift removed; settles 1.05--1.18\\
\addlinespace
Front stiffness underestimated &
Slip angle built from the steering \emph{setpoint} &
Achieved road-wheel angle from \texttt{feedback/\allowbreak steering\_angle}, with
fallback &
$C_{f}$: 13725 $\rightarrow$ 18486 \si{\newton\per\radian} (truth 17198)\\
\addlinespace
Normal loads 10--15\% low &
$m$ copied from the configuration file (240\,kg) against a simulated
269.6\,kg; $l_{wb}\ne l_{f}+l_{r}$ &
Measured mass and geometry; $l_{wb}\equiv l_{f}+l_{r}$ enforced by test &
$F_{z,f}/F_{z,r}$: 1222/1134 $\rightarrow$ 1345.6/1298.9 \si{\newton}\\
\addlinespace
Friction prior pinned near 0.4 &
Median of \emph{utilised} friction over an explicitly \emph{low-slip} mask &
$P_{99}$ over the whole buffer, applied as a floor on the prior &
Median 0.044 vs peak 0.46 at \SI{13}{\meter\per\second}\\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Ablations}
\label{subsec:ablation}

Two ablation axes are exposed by the implementation and are marked as not yet
run rather than reported with placeholder numbers. The first is the residual
architecture (\texttt{baseline} MLP, \texttt{wide}, \texttt{physics\_inputs},
\texttt{ensemble}, \texttt{s4}), scored on one-step and 10-step RMSE and on the
identified coefficients; the relevant comparison
is~\cite{visionsysid2026}, which reports a lateral-force RMSE reduction
exceeding \SI{60}{\percent} for an S4 residual, and we expect a smaller effect
here because our sequences are short and our residual is dominated by a
correctable measurement bias rather than by unmodelled temporal dynamics. The
second is the loss mode (\texttt{mse} versus \texttt{physics\_informed}) on the
same dataset, comparing one-step metrics, per-iteration coefficient
trajectories and closed-loop tracking.

\subsection{Negative result: the raceline, not the model}
\label{subsec:negative}

With the identifier corrected, level 3 of the validation protocol
(Section~\ref{subsec:validation}) does \emph{not} complete, and the reason is
instructive. The identified model reports a grip ceiling of
\SIrange{1.02}{1.16}{\g}; the plant sustains $\approx\SI{1.0}{\g}$ in steady
state; the raceline demands
$\max(v_{x}^{2}|\kappa|)=\SI{12.00}{\meter\per\second\squared}=\SI{1.22}{\g}$
($P_{99}=\SI{11.90}{\meter\per\second\squared}$). The admissibility gate of
Section~\ref{sec:integration} therefore rejects every identification, and does
so correctly: the trajectory is infeasible for this vehicle, and the corrected
identifier is what made that visible.

The cause is worth naming because any model-based stack can inherit it. The
trajectory optimiser was handed a \texttt{ggv} envelope asserting a constant
\SI{12.0}{\meter\per\second\squared} of lateral capability and a vehicle mass
of \SI{204}{\kilo\gram}, and it did its job---the profile saturates that
envelope to the last digit. The envelope was not this vehicle's. Identification
and trajectory generation were parameterised from two different descriptions of
the same car, and nothing in the stack compared them until the gate did.

We do not loosen the gate. The quantitative consequence for regeneration is
that the tightest corner, $|\kappa|=\SI{0.0536}{\per\meter}$, permits
$\sqrt{g\mu/|\kappa|}=\SI{13.5}{\meter\per\second}$ at the measured grip
(\SIrange{13.7}{14.6}{\meter\per\second} at the identified ceiling of
\SIrange{1.02}{1.16}{\g}); capping the speed profile at
\SI{13}{\meter\per\second} brings peak demand to
\SI{9.05}{\meter\per\second\squared} (\SI{0.92}{\g}), whereas
\SI{15}{\meter\per\second} returns it to \SI{12.00}{\meter\per\second\squared}.
Regenerating the raceline for the \emph{measured} vehicle
($m=\SI{269.6}{\kilo\gram}$, $\mu_{\mathrm{peak}}=1.0$,
$v_{\max}\approx\SIrange{13}{14}{\meter\per\second}$) is the prerequisite for
the closed-loop experiment and is the immediate next step
(Section~\ref{sec:conclusion}).

\input{sections/figure_placeholders}
